# Build artifacts
bin/
obj/
TestResults/
*.user
.vs/
out/
.tools/
# Rust
target/
# Secrets / local
*.local
.env
# Personal and machine-local files, wherever they sit: private notes and settings (*.local.md,
# settings.local.json), AI-assistant instructions and state, editor settings, caches and logs.
*.local.*
/CLAUDE.md
.claude/
.vscode/
.idea/
.ruff_cache/
*.log
# Scan reports exported by the CLI/dashboard (winsight-<yyyyMMdd-HHmmss>.json)
winsight-*.json
# Local evaluation runs
evals/out/
evals/results/

# Local-only VM qualification / diagnostic probe kits. These are privileged, exploratory
# harnesses that only run against a real machine (WFP/IPC), never in CI, and are not part
# of the shipped product or the unit test suite. Keep them out of the repo.
/tests/validation/
