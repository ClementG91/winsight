# Published v0.12.0 release verification - 2026-09-01

**Result: PASS for the published x64 artifacts and the architecture-independent release supply
chain.**

This record covers the assets published by release workflow `33497585184` at
[`v0.12.0`](https://github.com/ClementG91/winsight/releases/tag/v0.12.0). The annotated tag points
to main commit `b1c46eef53dbdc33d8da5498c6fae3a74bcad027`. It supplements, rather than replaces, the
privileged native-x64 candidate campaign at `dbaded1`.

The validation-only follow-up passed CI run `33525240961` and CodeQL run `33525237871`, then merged
to main as `18c17207e235ea4747ddf15398085e8e1bd35824`. The resulting main state passed CI run
`33526181382` and CodeQL run `33526181078`.

## Published asset identity

| Asset | SHA-256 |
|---|---|
| `winsight-v0.12.0-win-x64.zip` | `1B307B35F358C933B84C599C2D969FD640FBCE32DCFB9BF5AD1BDE860522DBD4` |
| `winsight-v0.12.0-win-x64-setup.exe` | `09715A96D74E5C08140BC2B5321D4D1903344835C7A4E04D8F2C89748F2DE2DD` |
| `winsight-v0.12.0-win-x64.spdx.json` | `AA6B38A9A925A71449183DC33C01CC0E7F913A0C4A7EC917C62D0D3AFA5F0E76` |
| `winsight-v0.12.0-win-arm64.zip` | `8061DD7390C69738EEEE1B50DB61E80375171B466936DD281C652838442FA944` |
| `winsight-v0.12.0-win-arm64-setup.exe` | `CA1AA52EB8EF4856A8F366900861B997678FB009B31161A31E349E30140A707B` |
| `winsight-v0.12.0-win-arm64.spdx.json` | `B0E439DEA5C6019B72A9CD16AF218E2A012BA37D22AC48260B834E9E5E52A782` |

All six downloaded assets matched their separately published `.sha256` files. Both SBOM documents
parsed as SPDX 2.2 with the expected `CC0-1.0` data license, WinSight 0.12.0 package identity, files
and packages. The x64 and Arm64 ZIPs each contained the expected three executables; their PE machine
values were respectively `0x8664` and `0xAA64`. The native x64 CLI returned
`winsight 0.12.0`.

GitHub attestation verification passed twice for each ZIP and setup executable: once for SLSA build
provenance (`https://slsa.dev/provenance/v1`) and once for the SPDX inventory
(`https://spdx.dev/Document/v2.2`). The standalone SBOM files are checksum-bound release assets;
they are predicates for the ZIP/setup attestations, not themselves attestation subjects.

## Published x64 installer smoke

The downloaded x64 setup was mounted read-only into the visible Windows 11 qualification VM and
run through the repository's `Test-Installer.ps1` lifecycle using Windows PowerShell 5.1. It passed:

- silent French installation into a unique temporary directory;
- native x64 validation of the CLI, dashboard and firewall service;
- exact `winsight 0.12.0` version output;
- MCP 0.12.0 stdio negotiation and read-only tool contract;
- dashboard smoke in English, French and Spanish;
- installed SPDX manifest and branding inventory checks;
- silent uninstall and bounded cleanup.

An independent post-run check found zero `winsight-installer-test-*` directories, zero running
WinSight processes and no `WinSightFirewall` service (`sc.exe` error 1060). The temporary read-only
VirtualBox share was removed after the run. The VM remained on protected snapshot
`S1-dbaded1-final-candidate-protected`.

## Scope limits

The two setup files and all six shipped product executables reported `NotSigned`, as required by the
documented temporary unsigned-distribution policy. Native Arm64 build, package and installer
lifecycle passed in GitHub's native Arm64 runner, and the downloaded Arm64 PE identities and supply
chain passed here. Privileged Arm64 WFP/SCM/trust/IPC/session behavior and x64-on-Arm64 application
identity remain `NOT_RUN` until suitable isolated Arm64 hardware is available.
